import { SensitiveDataDirective } from './sensitive-data.directive';

describe('SensitiveDataDirective', () => {
  it('should create an instance', () => {
    const directive = new SensitiveDataDirective();
    expect(directive).toBeTruthy();
  });
});
